## 1. Host Query Layer

- [x] 1.1 Add read-only OpenClaw query commands for plugin info and plugin config
- [x] 1.2 Add parsers for formatted plugin info output and warning-prefixed config JSON

## 2. Doctor Integration

- [x] 2.1 Extend OpenClaw install inspection to include live host plugin status and config match checks
- [x] 2.2 Update `ee doctor` output to surface live host status, host errors, and config mismatch state

## 3. Validation

- [x] 3.1 Add unit tests with representative real OpenClaw output samples
- [x] 3.2 Keep the existing installer and plugin regression suite green
