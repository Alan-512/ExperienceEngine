## 1. Recent Query Support

- [x] 1.1 Add repository support for recent history filtering and limits

## 2. CLI Inspect Parsing

- [x] 2.1 Implement `ee inspect recent injected`
- [x] 2.2 Implement `ee inspect recent <limit>` and `ee inspect recent injected <limit>`
- [x] 2.3 Keep invalid forms on a short usage message

## 3. Validation

- [x] 3.1 Add unit tests for injected-only and limited recent history views
- [x] 3.2 Run `pnpm check` and `openspec validate --changes --strict`
