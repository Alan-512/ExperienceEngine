## 1. Scope Management

- [x] 1.1 Implement `ee enable scope` using the existing current-directory scope resolution
- [x] 1.2 Update CLI routing and usage copy for the new command

## 2. Validation

- [x] 2.1 Add unit tests for enabling disabled and already-enabled scopes
- [x] 2.2 Run `pnpm check` and `openspec validate --changes --strict`
